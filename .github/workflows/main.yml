name: deploy-lovable
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: build
        run: |
          /usr/local/bin/bun install
          /usr/local/bin/bun run build
      - name: sync
        run: |
          sudo /usr/bin/mkdir -p /var/www/html/meu-site
          sudo /usr/bin/cp -r dist/. /var/www/html/meu-site/
